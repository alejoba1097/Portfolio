# Use the following code in case AWS CLI is not installed
# AWS CLI will use the config and credentials files in ~/.aws


/*
provider "aws" {
    region = "us-east-1"
    access_key = ""
    secret_key = ""
}
*/